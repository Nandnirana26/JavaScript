"use strict";//treat all js code as newer version(automaticaally ho jata h)

// alert(2+3)//we are using nodejs, not browser

// we avoid semicolon in react and js most imp is readability
console.log(3+3);console.log("nandni")//in real word this is bad

// spaces are automatically ignore in js
// readinility should be high
// Documentation--->tc3(ecma script(standared leakhe h kaise use karne h) )not easy only for browser, MDN

let name="Nandni"//string
let age=20//number
let isLoggedIn=false//boolean


// primitives datatypes

//number=> 2 to power 53
// bigInt=>number badaa ho agar
// string=>""
// boolean=>true/false
// null=>standalone value(type)->special type(representation of empty value)//lhali string alag h defined h string but null is different type
// undefined=>variable declare but not defined
// symbol=>when we use react(for uniqueness)

// objects

console.log(typeof "nandni");

console.log(typeof null);//object but undefined is undefined bcz its a type






